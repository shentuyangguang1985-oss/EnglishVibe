# 🔧 EnglishVibe 问题解决指南

本文档记录了开发和使用过程中遇到的所有问题及其解决方案，供参考。

---

## 目录

1. [音频相关问题](#1-音频相关问题)
2. [部署相关问题](#2-部署相关问题)
3. [PWA 相关问题](#3-pwa-相关问题)
4. [移动端适配问题](#4-移动端适配问题)
5. [功能相关问题](#5-功能相关问题)
6. [浏览器兼容性问题](#6-浏览器兼容性问题)

---

## 1. 音频相关问题

### 1.1 单词发音效果差

**问题描述：**  
使用 Web Speech API 的单词发音质量不佳，声音机械不自然。

**解决方案：**  
切换到有道词典 API 获取真人发音：
```javascript
// 使用有道词典API
const url = `https://dict.youdao.com/dictvoice?audio=${encodeURIComponent(word)}&type=1`;
const audio = new Audio(url);
audio.play();
```

---

### 1.2 例句发音质量差 / 无声音

**问题描述：**  
例句发音尝试了多种方案都效果不佳：
- Web Speech API：声音机械
- 有道词典 API：只支持单词，长句报错
- 有道翻译 API：返回 688 错误
- Google Translate TTS：国内无法访问

**解决方案：**  
最终采用讯飞 TTS API（WebSocket 方式）：

1. 在 [讯飞开放平台](https://www.xfyun.cn) 注册账号
2. 创建应用，开通「在线语音合成」服务
3. 添加英语发音人（如 Luna）
4. 获取 APPID、API Key、API Secret
5. 使用 `js/xfyun-tts.js` 模块调用

**关键代码：**
```javascript
// 讯飞TTS配置
const config = {
  appId: 'your_app_id',
  apiKey: 'your_api_key',
  apiSecret: 'your_api_secret'
};

// 发音人参数（Luna英语女声）
vcn: 'x4_enus_luna_assist'
```

---

### 1.3 讯飞 TTS 401 认证失败

**问题描述：**  
调用讯飞 TTS API 时返回 401 Unauthorized 错误。

**原因分析：**  
- API Secret 复制错误（多了空格或少了字符）
- 签名算法实现不正确

**解决方案：**  
1. 仔细核对 API Secret（从讯飞控制台复制）
2. 严格按照讯飞文档实现 HMAC-SHA256 签名
3. 确保 URL 编码正确

---

### 1.4 讯飞 TTS 11200 错误

**问题描述：**  
调用讯飞 TTS 返回 `Error code 11200: lisc failed`

**原因分析：**  
发音人参数（vcn）错误，未在控制台启用对应语音。

**解决方案：**  
1. 登录讯飞控制台
2. 进入应用 → 语音合成 → 添加发音人
3. 选择英语发音人（如 lindsay 或 luna）
4. 使用正确的 vcn 参数，例如：
   - `lindsay` - 英语女声
   - `x4_enus_luna_assist` - Luna 英语女声（更自然）

---

### 1.5 Base64 解码失败

**问题描述：**  
讯飞 TTS 返回的音频数据解码时报错：
```
Failed to execute 'atob': The string to be decoded is not correctly encoded
```

**原因分析：**  
讯飞返回的 Base64 数据分多个 chunk，直接拼接后解码会出错。

**解决方案：**  
逐个 chunk 解码，然后合并：
```javascript
// 逐块解码
const audioChunks = [];
for (const chunk of base64Chunks) {
  const cleanChunk = chunk.replace(/\s/g, '');
  const binaryStr = atob(cleanChunk);
  const bytes = new Uint8Array(binaryStr.length);
  for (let i = 0; i < binaryStr.length; i++) {
    bytes[i] = binaryStr.charCodeAt(i);
  }
  audioChunks.push(bytes);
}

// 合并为 Blob
const blob = new Blob(audioChunks, { type: 'audio/mp3' });
const audioUrl = URL.createObjectURL(blob);
```

---

### 1.6 音频播放 NotSupportedError

**问题描述：**  
播放音频时报错：
```
NotSupportedError: Failed to load because no supported source was found
```

**原因分析：**  
使用 `data:audio/mp3;base64,xxx` 格式在某些浏览器不兼容。

**解决方案：**  
改用 Blob URL 方式播放：
```javascript
// 不推荐
const audio = new Audio('data:audio/mp3;base64,' + base64Data);

// 推荐
const blob = new Blob([audioData], { type: 'audio/mp3' });
const url = URL.createObjectURL(blob);
const audio = new Audio(url);
```

---

### 1.7 第二遍发音与用户操作冲突

**问题描述：**  
单词读第二遍时，用户已经答题完成点击"下一个"，导致音频混乱。

**解决方案：**  
在 `nextWord()` 函数开头调用 `Audio.stop()` 停止所有音频：
```javascript
nextWord() {
  Audio.stop();  // 停止当前音频
  // ... 加载下一个单词
}
```

---

## 2. 部署相关问题

### 2.1 Vercel 部署后手机打不开

**问题描述：**  
电脑可以访问 Vercel 部署的网站，但手机一直加载中。

**原因分析：**  
- Vercel 服务器在国外，国内访问不稳定
- 词库文件过多（59个JSON文件），加载慢

**解决方案：**  
1. 合并词库文件为单个 `vocab_all.json`
2. 改用 Cloudflare Pages 部署（国内访问更快）

---

### 2.2 Cloudflare Pages 构建失败

**问题描述：**  
部署到 Cloudflare Pages 时构建失败，日志显示需要 wrangler 配置。

**解决方案：**  
在项目根目录创建 `wrangler.jsonc`：
```json
{
  "name": "englishvibe",
  "compatibility_date": "2025-01-01",
  "assets": {
    "directory": "./"
  }
}
```

---

### 2.3 代码更新后网站未更新

**问题描述：**  
推送代码到 GitHub 后，网站显示的还是旧版本。

**原因分析：**  
- Service Worker 缓存了旧文件
- 浏览器缓存

**解决方案：**  
1. 更新 `sw.js` 中的 `CACHE_NAME` 版本号：
   ```javascript
   const CACHE_NAME = 'englishvibe-v49';  // 递增版本号
   ```
2. 清除浏览器缓存
3. 在设置页点击"清除所有数据"

---

## 3. PWA 相关问题

### 3.1 "安装到桌面"按钮不显示

**问题描述：**  
首页没有看到"安装到桌面"按钮。

**原因分析：**  
- 之前按钮是动态创建的，依赖 `beforeinstallprompt` 事件
- 某些浏览器不触发该事件

**解决方案：**  
在 HTML 中直接添加按钮，并提供手动安装提示：
```html
<button class="btn btn--secondary" id="btn-install">
  <span class="btn__icon">📲</span>
  安装到桌面
</button>
```

```javascript
async function installApp() {
  if (deferredPrompt) {
    deferredPrompt.prompt();
  } else {
    // 显示手动安装说明
    alert('请使用浏览器菜单中的"添加到主屏幕"选项');
  }
}
```

---

### 3.2 iOS 无法安装 PWA

**问题描述：**  
iPhone 用户找不到"安装应用"选项。

**原因分析：**  
iOS 只有 Safari 浏览器支持 PWA 安装。

**解决方案：**  
1. 必须使用 **Safari** 浏览器打开
2. 点击底部 **分享按钮** ⬆️
3. 选择 **"添加到主屏幕"**

---

### 3.3 Service Worker 更新不生效

**问题描述：**  
代码已更新，但用户设备上还是旧版本。

**解决方案：**  
在 `index.html` 中添加自动更新检测：
```javascript
registration.addEventListener('updatefound', () => {
  const newWorker = registration.installing;
  newWorker.addEventListener('statechange', () => {
    if (newWorker.state === 'installed' && navigator.serviceWorker.controller) {
      window.location.reload();
    }
  });
});
```

---

## 4. 移动端适配问题

### 4.1 内容一屏显示不下

**问题描述：**  
手机上页面底部内容被截断，需要滚动才能看到。

**解决方案：**  
添加响应式 CSS 媒体查询：
```css
@media (max-height: 750px) {
  .word-card__image { height: 140px; }
  .option { min-height: 50px; }
  /* ... 其他压缩样式 */
}

@media (max-height: 680px) {
  .word-card__image { height: 100px; }
  /* ... 更紧凑的样式 */
}

@media (max-height: 600px) {
  /* ... 极限压缩样式 */
}
```

---

### 4.2 内容集中在顶部

**问题描述：**  
页面内容都挤在顶部，底部大片空白。

**解决方案：**  
为 `.main` 添加垂直居中：
```css
.main {
  display: flex;
  flex-direction: column;
  justify-content: center;  /* 垂直居中 */
}
```

---

### 4.3 刘海屏适配

**问题描述：**  
在有刘海的手机上，内容被刘海遮挡。

**解决方案：**  
使用 CSS 安全区：
```css
@supports (padding-top: env(safe-area-inset-top)) {
  .header {
    padding-top: calc(var(--spacing-md) + env(safe-area-inset-top));
  }
  
  .footer-info {
    padding-bottom: calc(var(--spacing-lg) + env(safe-area-inset-bottom));
  }
}
```

---

## 5. 功能相关问题

### 5.1 "继续学习"按钮无反应

**问题描述：**  
完成今日目标后，点击"继续学习"没有反应。

**原因分析：**  
`learn.js` 检测到已完成今日目标，直接跳转到完成页。

**解决方案：**  
添加"继续学习"模式，通过 URL 参数跳过目标检测：
```javascript
// complete.html 中
window.location.href = './learn.html?continue=true';

// learn.js 中
const urlParams = new URLSearchParams(window.location.search);
this.state.continueMode = urlParams.get('continue') === 'true';

if (this.state.continueMode) {
  // 忽略今日目标，继续加载新单词
}
```

---

### 5.2 "返回首页"按钮无反应

**问题描述：**  
复习页面的"返回首页"按钮点击无效。

**原因分析：**  
当没有待复习单词时，`bindEvents()` 函数未被调用。

**解决方案：**  
将导航按钮绑定移到 `init()` 函数开头，确保始终执行：
```javascript
init() {
  this.bindBasicEvents();  // 先绑定导航按钮
  // ... 其他初始化
}

bindBasicEvents() {
  document.getElementById('btn-back').addEventListener('click', () => {
    window.location.href = './index.html';
  });
}
```

---

### 5.3 词库包含小学词汇

**问题描述：**  
词库中有 am、his、I、nice 等小学就学过的简单词汇。

**解决方案：**  
1. 重新整理词库，严格按照初中课标筛选
2. 移除所有小学阶段词汇
3. 合并外研版和人教版教材词汇
4. 最终保留 2276 个初中核心词汇

---

## 6. 浏览器兼容性问题

### 6.1 可选链操作符不支持

**问题描述：**  
部分旧版手机浏览器报错：
```
Unexpected token '?'
```

**原因分析：**  
ES6 的可选链操作符 `?.` 在旧浏览器不支持。

**解决方案：**  
使用传统的 if 判断代替：
```javascript
// 不兼容
element?.addEventListener('click', handler);

// 兼容写法
if (element) {
  element.addEventListener('click', handler);
}
```

---

### 6.2 Audio 构造函数冲突

**问题描述：**  
讯飞 TTS 模块中 `new Audio()` 报错：
```
TypeError: Audio is not a constructor
```

**原因分析：**  
项目中有一个 `Audio` 模块，与全局 `Audio` 构造函数冲突。

**解决方案：**  
使用 `window.Audio` 明确引用全局构造函数：
```javascript
const audio = new window.Audio(url);
```

---

## 💡 通用排查步骤

遇到问题时，可以按以下步骤排查：

1. **打开浏览器开发者工具**（F12）
2. **查看 Console 错误信息**
3. **检查 Network 请求状态**
4. **清除缓存和数据**
5. **尝试无痕模式打开**
6. **更新 Service Worker 版本号**
7. **重新部署**

---

## 📞 获取帮助

如果以上方案无法解决问题，请：

1. 在 [GitHub Issues](https://github.com/shentuyangguang1985-oss/EnglishVibe/issues) 提交问题
2. 附上以下信息：
   - 设备型号和操作系统版本
   - 浏览器名称和版本
   - 问题截图
   - Console 错误日志

---

*最后更新：2025-01-01*

